version: "3"

services:
  nginx-php:
    build: ../.
    container_name: nginx-php
    ports:
      - "80:80"
    volumes:
      - "./html:/www"
      - "./nginx:/etc/nginx/conf.d/"
#      - "./volumes/certs:/etc/nginx/certs:ro"